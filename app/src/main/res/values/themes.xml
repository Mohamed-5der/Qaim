<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Qaim" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/teal_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/elmessiri_regular</item>
    </style>
    
    <style name="DialogAnimation" >
        <item name="android:windowEnterAnimation">@anim/bottom_sheet_out</item>
        <item name="android:windowExitAnimation">@anim/bottom_sheet_anim</item>
    </style>

    <style name="ImageDialogAnimation" >
        <item name="android:windowEnterAnimation">@anim/bottom_sheet_out</item>
        <item name="android:windowExitAnimation">@anim/bottom_sheet_anim</item>
    </style>

    <style name="alertAnimation" >
        <item name="android:windowEnterAnimation">@anim/alert_animation</item>
        <item name="android:windowExitAnimation">@anim/alert_close_animation</item>
    </style>

</resources>